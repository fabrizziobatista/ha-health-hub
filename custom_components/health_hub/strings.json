{
  "config": {
    "step": {
      "user": {
        "title": "Health Hub",
        "description": "Enable the Health Hub runtime. Systems and checks are configured in YAML."
      }
    },
    "abort": {
      "already_configured": "Health Hub is already configured."
    }
  }
}
